# empty string
var s = ""
assert_eq(len(s), 0, "strings: empty length == 0")
remove s

# repeat/edge: small string repeat
var a = "a"
var r = a * 3
assert_eq(r, "aaa", "strings: repeat produces 'aaa'")
remove a, r

var c = "\""
assert_eq(c, '"', "string: is escaped")
remove c
